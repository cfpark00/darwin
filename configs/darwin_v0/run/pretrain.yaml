# Darwin - Pretrain Run Configuration

output_dir: "data/darwin_v0/run_pretrain"
world_config: "configs/darwin_v0/world/pretrain.yaml"

seed: 42
max_steps: 10000
min_buffer_size: 4096

logging:
  detailed_interval: 250
  checkpoint_interval: 2500
