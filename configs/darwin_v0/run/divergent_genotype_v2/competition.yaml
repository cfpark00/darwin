# Competition experiment - Cluster 5 vs Cluster 17 head-to-head
# Part of divergent_genotype_v2 group
#
# Hypothesis: One cluster may outcompete the other through
# better foraging efficiency, reproduction timing, or movement strategies

output_dir: "data/darwin_v0/divergent_genotype_v2/competition"
world_config: "configs/darwin_v0/world/divergent_genotype_v2/competition.yaml"

seed: 42
max_steps: 5000
min_buffer_size: 1024

logging:
  detailed_interval: 500
  checkpoint_interval: 500

transfer:
  checkpoint_path: "data/darwin_v0/run_from_pretrain_simple/checkpoints/ckpt_050001.pkl"
  cluster_assignments_path: "scratch/genotype_analysis_v1/cluster_assignments.npz"
  cluster_id_a: 5
  cluster_id_b: 17
  num_agents_per_cluster: 256
  initial_energy: 100.0
  spawn_region: [64, 192, 64, 192]  # 128x128 centered region (mixed spawning)
