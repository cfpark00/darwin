# Darwin v0.0 - Run Configuration

output_dir: "data/darwin_v0/run_default"
world_config: null  # null = use default world_config.yaml

seed: 42
max_steps: 50000
min_buffer_size: 4096

logging:
  detailed_interval: 250
  checkpoint_interval: 2500
