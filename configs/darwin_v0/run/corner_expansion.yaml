# Corner expansion experiment
# Agents start in corner box, spread with reproduction enabled, no resource regen

output_dir: "data/darwin_v0/corner_expansion"
world_config: "configs/darwin_v0/world/corner_expansion_simple.yaml"

seed: 42
max_steps: 10000
min_buffer_size: 256

logging:
  detailed_interval: 50
  checkpoint_interval: 50

transfer:
  checkpoint_path: "data/darwin_v0/run_from_pretrain_simple/checkpoints/ckpt_050001.pkl"
  num_agents: 256
  initial_energy: 100.0
  spawn_region: [0, 16, 0, 16]  # 16x16 grid at bottom-left corner
