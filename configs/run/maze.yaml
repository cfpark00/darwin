# Darwin - Maze Transfer Experiment
# Load evolved agents from a checkpoint and place in maze environment

output_dir: "data/maze"
world_config: "configs/world/maze.yaml"

# Source checkpoint to load agents from
transfer:
  checkpoint_path: "data/run_default_archived/checkpoints/ckpt_100000.pkl"
  num_agents: 100  # Number of random agents to sample
  initial_energy: 100.0  # Energy to give transferred agents
  spawn_region: [0, 32, 0, 32]  # [y_min, y_max, x_min, x_max] - start cell (bottom-left)

seed: 0
max_steps: 10000
min_buffer_size: 4096

logging:
  detailed_interval: 250
  checkpoint_interval: 250
