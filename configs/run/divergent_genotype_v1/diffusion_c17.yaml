# Diffusion experiment - Cluster 17 (n=1040, left side of PC1)
# Part of divergent_genotype_v1 group

output_dir: "data/divergent_genotype_v1/diffusion_c17"
world_config: "configs/world/divergent_genotype_v1/diffusion.yaml"

seed: 42
max_steps: 1000
min_buffer_size: 256

logging:
  detailed_interval: 100
  checkpoint_interval: 100

transfer:
  checkpoint_path: "data/run_from_pretrain_simple/checkpoints/ckpt_050001.pkl"
  cluster_assignments_path: "data/reference/genotype_clusters_v1/cluster_assignments.npz"
  cluster_id: 17
  num_agents: 256
  initial_energy: 100.0
  spawn_region: [120, 136, 120, 136]  # 16x16 grid centered
