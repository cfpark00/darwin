# Chemotaxis experiment - Cluster 5 (circling strategy)

output_dir: "data/divergent_genotype_v1/chemotaxis_c5"
world_config: "configs/world/divergent_genotype_v1/chemotaxis.yaml"

seed: 42
max_steps: 2000
min_buffer_size: 256

logging:
  detailed_interval: 200
  checkpoint_interval: 200

transfer:
  checkpoint_path: "data/run_from_pretrain_simple/checkpoints/ckpt_050001.pkl"
  cluster_assignments_path: "data/reference/genotype_clusters_v1/cluster_assignments.npz"
  cluster_id: 5
  num_agents: 256
  initial_energy: 30.0
  spawn_region: [120, 136, 120, 136]  # Center
