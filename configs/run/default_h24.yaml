# Darwin v0.0 - Run Configuration (Hidden dim 24)

output_dir: "data/run_default_h24"
world_config: "configs/world/default_h24.yaml"

seed: 42
max_steps: 100000
min_buffer_size: 4096

logging:
  detailed_interval: 250
  checkpoint_interval: 2500
